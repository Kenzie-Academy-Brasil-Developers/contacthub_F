import { FormLogin } from "../../components/formLogin"

export const Login = () => {
    return (
            <main>
                <div>
                    <h1>Login</h1>
                    <FormLogin />
                </div>
            </main>
        )
}